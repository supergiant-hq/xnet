syntax="proto3";
option go_package="/model";

package model;

message StreamConnectionData {
    string id = 1;
    map<string, string> metadata = 2;
    map<string, string> data = 3;
}

message StreamConnectionStatus {
    string id = 1;
    bool status = 2;
    string message = 3;
}